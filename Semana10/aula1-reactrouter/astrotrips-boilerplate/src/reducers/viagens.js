const initialState = {

    listaViagens: []

};


export const reducerViagens = ( state = initialState, action) => {
    switch(action.type){
        case "MOSTRAR_VIAGENS":

        default:
            return state;
    }
}